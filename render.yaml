services:
  - type: web
    name: netdefend-ai-backend
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app.filter_api:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0